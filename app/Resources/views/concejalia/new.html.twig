{% extends 'base.html.twig' %}

{% block body %}
    <h1>Concejalium creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Registrar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('concejalia_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
